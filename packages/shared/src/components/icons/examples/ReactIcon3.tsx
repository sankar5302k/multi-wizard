import ReactIconSingle from '../../../assets/icons/examples/react-option3-single.svg?react';
import { type CustomSVGIconType } from '../../../type';

const ReactIconOption3: CustomSVGIconType = ({ className }) => {
  return <ReactIconSingle className={className} />;
};

export default ReactIconOption3;
